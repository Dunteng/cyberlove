// import { notFound } from 'next/navigation'

export default function Blog() {
  // 示例：检查博客文章是否存在
  // const post = await fetchPost(params.slug) // 假设这是你的数据获取函数

  // if (!post) {
  //   notFound() // 这会触发 blog/not-found.tsx
  // }
  return <div>Blog</div>
}